public class LECTURETWO {
    /*
    FUNCTION STRUCTURE
    accessModifier returnType function(datatype paramName, datatype paramName){
        //Write your code here
        return value;
    }
   */
    /*
    Two ways to create a variable:
        - Variable Definition: Creates a variable with no value
        - Variable Initialization: Creates a variable with a value

    Variable Structure:
        - dataType variableName; //Defining a variable
        - dataType variableName = value; //Initializing a variable
    */

    //= != ==
        /*
        ASSIGNMENT OPERATORS
        == : Compares if the values are the same
        > : Compares if the first value is greater than the second value
        < : Compares if the first value is less than the second value
        >= : Compares if the first value is greater than or equal to the second value
        <= : Compares if the first value is less than or equal to the second value
        != : Compares if the values are not the same
        */

        /*
        LOGICAL OPERATORS
        && - Check if both values are true
        || - Checks if at least one of the values is true
        ! - Check if the value is the opposite || Assigns a value its opposite
        !true = false
        !false = true
        */

    //Main method - starting point of every java program
    public static void main(String[] args) {


    }
}
